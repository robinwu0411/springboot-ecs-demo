spring:
  datasource:
    url: jdbc:mysql://db1.cz80s4oywgp4.ap-northeast-1.rds.amazonaws.com:3306/test1?sslMode=DISABLED&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&connectTimeout=5000&socketTimeout=5000
    username: admin
    password: w1111111
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 3000
      initialization-fail-timeout: 3000

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.demo.crud.model
  configuration:
    map-underscore-to-camel-case: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.demo.crud: DEBUG
